/**
 * > greater than
 * >= greater equal than
 * <  less than
 * <= less equal than
 * == equal(check only the value) ********** not commonly used
 * === equal(check the value and the type)
 * !=  not equal(check only the value) ********** not commonly used
 * !== not equal(check the value and the type)
 */