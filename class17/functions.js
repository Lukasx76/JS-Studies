// old school method to define a function

function sum(x, y){
    return x + y
};

// enchanced method to define a function

const multiplication = function(x, y){
    return x * y
};

// more enchanced method to define a function

const subtraction = (x, y) => {return x - y};

// lazy way to define a function(pure arrow function)

const root = n => n ** 0.5;


