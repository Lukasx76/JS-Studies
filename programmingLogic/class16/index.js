// learning about for of

// this type of for returns only the value of the iterable
// for in return just the index

const cars = ["dodge challenger", "mustang shelby", "nissan skyline", "ferrari enzo"];

for (let value of cars){
    console.log(value);
}