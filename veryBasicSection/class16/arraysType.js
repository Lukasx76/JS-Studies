let fruitsArray = ["apple", "orange"]; // defining the array

fruitsArray.push("banana"); // adding to the final
fruitsArray.unshift("grape"); // adding to the beginning
fruitsArray.slice(0, fruitsArray.length + 1); // slicing the array
fruitsArray.pop(); // removing the last element
fruitsArray.shift(); // removing the first element